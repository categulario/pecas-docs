.\" Automatically generated by Pandoc 2.5
.\"
.TH "" "pc-recreator" "" "Véase también: pc-recreator -h" "Pecas"
.hy
.SH Uso de \f[C]pc\-recreator\f[R]
.PP
Recreator recrea los archivos OPF, NCX y NAV as\['i] como crea o recrea
el archivo EPUB.
.SS Dependencias necesarias:
.PP
\f[C]zip\f[R]
.SS Uso:
.PP
pc\-recreator
.SS Par\['a]metros opcionales:
.PP
\f[C]\-d\f[R] = [directory] Directorio del proyecto EPUB.
.PP
\f[C]\-y\f[R] = [yaml] Archivo de los metadatos para el EPUB.
.PP
\f[C]\-\-depth\f[R] = N\['u]mero entero que indica el nivel de
profundidad de la tabla de contenidos.
.SS Par\['a]metros \['u]nicos:
.PP
\f[C]\-v\f[R] = [version] Muestra la versi\['o]n.
.PP
\f[C]\-h\f[R] = [help] Muestra esta ayuda.
bits.
.SS Ejemplo sencillo:
.PP
pc\-recreator
.PP
Crea un archivo EPUB buscando dentro del directorio actual los ficheros
\f[C]epub\-creator\f[R] y \f[C]meta\-data.yaml\f[R].
.SS Ejemplo con un proyecto EPUB espec\['i]fico:
.PP
pc\-recreator \-d directorio/para/epub
.PP
Crea un archivo EPUB de \f[C]directorio/para/epub\f[R] buscando dentro
del directorio actual el fichero \f[C]meta\-data.yaml\f[R].
.SS Ejemplo con un proyecto EPUB y metadatos espec\['i]ficos:
.PP
pc\-recreator \-d directorio/para/epub \-y archivo/meta\-data.yaml
.PP
Crea un archivo EPUB de \f[C]directorio/para/epub\f[R] usando el fichero
\f[C]archivo/meta\-data.yaml\f[R].
.SS Ejemplo con un proyecto EPUB, metadatos espec\['i]ficos y profundidad:
.PP
pc\-recreator \-d directorio/para/epub \-y archivo/meta\-data.yaml
\-\-depth 4
.PP
Crea un archivo EPUB de \f[C]directorio/para/epub\f[R] usando el fichero
\f[C]archivo/meta\-data.yaml\f[R] y con una tabla de contenidos con
hasta encabezados \f[C]h4\f[R].
